const monthEl = document.querySelector('.js-month');
const dayEl = document.querySelector('.js-day');
const dayNumberEl = document.querySelector('.js-day-number');
const yearsEl = document.querySelector('.js-year');

export default { monthEl, dayEl, dayNumberEl, yearsEl };
