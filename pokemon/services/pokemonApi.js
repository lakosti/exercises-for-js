// async function fetchPokemon(pokemonId) {
//     const resp = await fetch(`https://pokeapi.co/api/v2/pokemon/${pokemonId.toLowerCase()}/`); // у фечі ми витягуваємо дані і збегіраємо у змінну тому await вказуємо
//     return resp.json(); // await не пишемо оскільки не використовуємо в цій функції
// }

// export { fetchPokemon }; // named export
